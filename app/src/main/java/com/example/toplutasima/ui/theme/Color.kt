package com.example.toplutasima.ui.theme

import androidx.compose.ui.graphics.Color

// Brand
val TransitBlue = Color(0xFF3D8EFF)
val TransitBlueDim = Color(0xFF1E4A8C)

// Backgrounds
val BackgroundDark = Color(0xFF0F1117)
val SurfaceCard = Color(0xFF1A1D27)
val SurfaceElevated = Color(0xFF242837)

// Text
val TextPrimary = Color(0xFFEEF0F7)
val TextSecondary = Color(0xFF8A8FA8)
val TextMuted = Color(0xFF4A4F66)

// Semantic
val SuccessGreen = Color(0xFF2ECC7A)
val WarningAmber = Color(0xFFFFA040)
val DangerRed = Color(0xFFFF5E5E)

// Line type chips
val DirectLineColor = Color(0xFF2ECC7A)
val TransferLineColor = Color(0xFFFFA040)

// Compatibility tokens used by existing UI components.
val SurfaceL0 = BackgroundDark
val SurfaceL1 = SurfaceCard
val SurfaceL2 = SurfaceElevated
val SurfaceD0 = BackgroundDark
val SurfaceD1 = SurfaceCard
val SurfaceD2 = SurfaceElevated

val TextHighLight = TextPrimary
val TextHighDark = TextPrimary
val TextMidLight = TextSecondary
val TextMidDark = TextSecondary
val TextLowLight = TextMuted
val TextLowDark = TextMuted

val AccentLight = TransitBlue
val AccentDark = TransitBlue
val AccentSoftL = TransitBlueDim
val AccentSoftD = TransitBlueDim

val GreenLight = SuccessGreen
val GreenDark = SuccessGreen
val AmberLight = WarningAmber
val AmberDark = WarningAmber
val RedLight = DangerRed
val RedDark = DangerRed

val BorderLight = TextMuted
val BorderDark = TextMuted
