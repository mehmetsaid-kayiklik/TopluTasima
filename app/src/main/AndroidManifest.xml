<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION"/>
    <!-- Kişisel Araç — Foreground Service için -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <!-- Toplu Taşıma Bildirimleri -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.USE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE"/>

    <application
        android:name=".TopluTasimaApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TopluTasima">
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:label="@string/app_name"
            android:theme="@style/Theme.TopluTasima" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Kişisel Araç — GPS Waypoint Takip Servisi -->
        <service
            android:name=".service.PersonalTripForegroundService"
            android:foregroundServiceType="location"
            android:exported="false"/>

        <!-- Toplu Taşıma — Bildirim Servisi -->
        <service
            android:name=".service.TransitTripForegroundService"
            android:foregroundServiceType="specialUse|location"
            android:exported="false">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Transit trip notification with boarding/alighting action buttons and arrival reminder"/>
        </service>

        <!-- Toplu Taşıma — Bildirim Aksiyon Receiver -->
        <service
            android:name=".service.JourneyMatchForegroundService"
            android:foregroundServiceType="location"
            android:exported="false"/>

        <receiver
            android:name=".service.TransitNotificationReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.toplutasima.transit.NOTIF_BINDIM"/>
                <action android:name="com.example.toplutasima.transit.NOTIF_INDIM"/>
                <action android:name="com.example.toplutasima.transit.REMINDER"/>
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.ActivityRecognitionReceiver"
            android:exported="false"/>

    </application>

</manifest>
