{
  "name": "toplutasima-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase --config ../firebase.json emulators:exec --only firestore,storage --project toplutasima-sprint7a-test \"node vehicleAssignments.rules.test.mjs && node vehiclePhotos.rules.test.mjs && node vehicleLedger.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "4.0.1",
    "firebase": "11.10.0",
    "firebase-tools": "14.7.0"
  }
}
